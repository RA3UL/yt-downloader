[phases.setup]
aptPkgs = ["curl", "ca-certificates"]

[phases.install]
cmds = ["curl -L https://github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp -o /usr/local/bin/yt-dlp && chmod a+rx /usr/local/bin/yt-dlp && npm install"]

[start]
cmd = "node server.js"
